language: cpp
sudo: required
dist: trusty

script:
    - mkdir build
    - cd build
    - cmake ..
    - make
    - cd ..
    - ./set_things_up.sh
    - ./download_videos.sh
    - ./run_basic_test.sh

before_script:
    - eval "${MATRIX_EVAL}"

matrix:
  include:

      ######## LINUX GCC ########
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-5
      env:
        - MATRIX_EVAL="CC=gcc-5 && CXX=g++-5"
      before_install:
        - sudo apt-key adv --keyserver keyserver.ubuntu.com --recv 5E6DA83306132997
        - sudo apt-add-repository "deb http://zeroc.com/download/apt/ubuntu$(lsb_release -rs) stable main"
        - sudo add-apt-repository ppa:mc3man/trusty-media -y
        - sudo apt-get update -y
        - sudo apt-get install zeroc-ice-all-runtime zeroc-ice-all-dev ffmpeg libtclap-dev
        - sudo apt-get install build-essential libpcre3 libpcre3-dev libssl-dev
        - pushd .
        - sudo mkdir ~/build-nginx && cd ~/build-nginx
        - sudo wget https://github.com/arut/nginx-rtmp-module/archive/v1.2.0.tar.gz
        - sudo wget https://nginx.org/download/nginx-1.13.3.tar.gz
        - sudo tar xzvf nginx-1.13.3.tar.gz
        - sudo tar xzvf v1.2.0.tar.gz
        - cd nginx-1.13.3
        - sudo ./configure --with-http_ssl_module --add-module=../nginx-rtmp-module-1.2.0
        - sudo make
        - sudo make install
        - sudo cp ~/build/Zialus/SD-Video-Streaming/extras/nginx.conf /usr/local/nginx/conf/
        - sudo /usr/local/nginx/sbin/nginx
        - popd

    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-6
      env:
        - MATRIX_EVAL="CC=gcc-6 && CXX=g++-6"
      before_install:
        - sudo apt-key adv --keyserver keyserver.ubuntu.com --recv 5E6DA83306132997
        - sudo apt-add-repository "deb http://zeroc.com/download/apt/ubuntu$(lsb_release -rs) stable main"
        - sudo add-apt-repository ppa:mc3man/trusty-media -y
        - sudo apt-get update -y
        - sudo apt-get install zeroc-ice-all-runtime zeroc-ice-all-dev ffmpeg libtclap-dev
        - sudo apt-get install build-essential libpcre3 libpcre3-dev libssl-dev
        - pushd .
        - sudo mkdir ~/build-nginx && cd ~/build-nginx
        - sudo wget https://github.com/arut/nginx-rtmp-module/archive/v1.2.0.tar.gz
        - sudo wget https://nginx.org/download/nginx-1.13.3.tar.gz
        - sudo tar xzvf nginx-1.13.3.tar.gz
        - sudo tar xzvf v1.2.0.tar.gz
        - cd nginx-1.13.3
        - sudo ./configure --with-http_ssl_module --add-module=../nginx-rtmp-module-1.2.0
        - sudo make
        - sudo make install
        - sudo cp ~/build/Zialus/SD-Video-Streaming/extras/nginx.conf /usr/local/nginx/conf/
        - sudo /usr/local/nginx/sbin/nginx
        - popd

    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-7
      env:
        - MATRIX_EVAL="CC=gcc-7 && CXX=g++-7"
      before_install:
        - sudo apt-key adv --keyserver keyserver.ubuntu.com --recv 5E6DA83306132997
        - sudo apt-add-repository "deb http://zeroc.com/download/apt/ubuntu$(lsb_release -rs) stable main"
        - sudo add-apt-repository ppa:mc3man/trusty-media -y
        - sudo apt-get update -y
        - sudo apt-get install zeroc-ice-all-runtime zeroc-ice-all-dev ffmpeg libtclap-dev
        - sudo apt-get install build-essential libpcre3 libpcre3-dev libssl-dev
        - pushd .
        - sudo mkdir ~/build-nginx && cd ~/build-nginx
        - sudo wget https://github.com/arut/nginx-rtmp-module/archive/v1.2.0.tar.gz
        - sudo wget https://nginx.org/download/nginx-1.13.3.tar.gz
        - sudo tar xzvf nginx-1.13.3.tar.gz
        - sudo tar xzvf v1.2.0.tar.gz
        - cd nginx-1.13.3
        - sudo ./configure --with-http_ssl_module --add-module=../nginx-rtmp-module-1.2.0
        - sudo make
        - sudo make install
        - sudo cp ~/build/Zialus/SD-Video-Streaming/extras/nginx.conf /usr/local/nginx/conf/
        - sudo /usr/local/nginx/sbin/nginx
        - popd

    ######## LINUX CLANG ########
    - os: linux
      addons:
        apt:
          sources:
            - llvm-toolchain-trusty-3.9
          packages:
            - clang-3.9
            - libstdc++6
      env:
        - MATRIX_EVAL="CC=clang-3.9 && CXX=clang++-3.9"
      before_install:
        - sudo apt-key adv --keyserver keyserver.ubuntu.com --recv 5E6DA83306132997
        - sudo apt-add-repository "deb http://zeroc.com/download/apt/ubuntu$(lsb_release -rs) stable main"
        - sudo add-apt-repository ppa:mc3man/trusty-media -y
        - sudo apt-get update -y
        - sudo apt-get install zeroc-ice-all-runtime zeroc-ice-all-dev ffmpeg libtclap-dev
        - sudo apt-get install build-essential libpcre3 libpcre3-dev libssl-dev
        - pushd .
        - sudo mkdir ~/build-nginx && cd ~/build-nginx
        - sudo wget https://github.com/arut/nginx-rtmp-module/archive/v1.2.0.tar.gz
        - sudo wget https://nginx.org/download/nginx-1.13.3.tar.gz
        - sudo tar xzvf nginx-1.13.3.tar.gz
        - sudo tar xzvf v1.2.0.tar.gz
        - cd nginx-1.13.3
        - sudo ./configure --with-http_ssl_module --add-module=../nginx-rtmp-module-1.2.0
        - sudo make
        - sudo make install
        - sudo cp ~/build/Zialus/SD-Video-Streaming/extras/nginx.conf /usr/local/nginx/conf/
        - sudo /usr/local/nginx/sbin/nginx
        - popd

    - os: linux
      addons:
        apt:
          sources:
            - llvm-toolchain-trusty-4.0
          packages:
            - clang-4.0
      env:
        - MATRIX_EVAL="CC=clang-4.0 && CXX=clang++-4.0"
      before_install:
        - sudo apt-key adv --keyserver keyserver.ubuntu.com --recv 5E6DA83306132997
        - sudo apt-add-repository "deb http://zeroc.com/download/apt/ubuntu$(lsb_release -rs) stable main"
        - sudo add-apt-repository ppa:mc3man/trusty-media -y
        - sudo apt-get update -y
        - sudo apt-get install zeroc-ice-all-runtime zeroc-ice-all-dev ffmpeg libtclap-dev
        - sudo apt-get install build-essential libpcre3 libpcre3-dev libssl-dev
        - pushd .
        - sudo mkdir ~/build-nginx && cd ~/build-nginx
        - sudo wget https://github.com/arut/nginx-rtmp-module/archive/v1.2.0.tar.gz
        - sudo wget https://nginx.org/download/nginx-1.13.3.tar.gz
        - sudo tar xzvf nginx-1.13.3.tar.gz
        - sudo tar xzvf v1.2.0.tar.gz
        - cd nginx-1.13.3
        - sudo ./configure --with-http_ssl_module --add-module=../nginx-rtmp-module-1.2.0
        - sudo make
        - sudo make install
        - sudo cp ~/build/Zialus/SD-Video-Streaming/extras/nginx.conf /usr/local/nginx/conf/
        - sudo /usr/local/nginx/sbin/nginx
        - popd

    - os: linux
      addons:
        apt:
          sources:
            - llvm-toolchain-trusty
          packages:
            - clang-5.0
      env:
        - MATRIX_EVAL="CC=clang-5.0 && CXX=clang++-5.0"
      before_install:
        - sudo apt-key adv --keyserver keyserver.ubuntu.com --recv 5E6DA83306132997
        - sudo apt-add-repository "deb http://zeroc.com/download/apt/ubuntu$(lsb_release -rs) stable main"
        - sudo add-apt-repository ppa:mc3man/trusty-media -y
        - sudo apt-get update -y
        - sudo apt-get install zeroc-ice-all-runtime zeroc-ice-all-dev ffmpeg libtclap-dev
        - sudo apt-get install build-essential libpcre3 libpcre3-dev libssl-dev
        - pushd .
        - sudo mkdir ~/build-nginx && cd ~/build-nginx
        - sudo wget https://github.com/arut/nginx-rtmp-module/archive/v1.2.0.tar.gz
        - sudo wget https://nginx.org/download/nginx-1.13.3.tar.gz
        - sudo tar xzvf nginx-1.13.3.tar.gz
        - sudo tar xzvf v1.2.0.tar.gz
        - cd nginx-1.13.3
        - sudo ./configure --with-http_ssl_module --add-module=../nginx-rtmp-module-1.2.0
        - sudo make
        - sudo make install
        - sudo cp ~/build/Zialus/SD-Video-Streaming/extras/nginx.conf /usr/local/nginx/conf/
        - sudo /usr/local/nginx/sbin/nginx
        - popd

    ######## OSX CLANG ########
    - os: osx
      osx_image: xcode9
      env:
        - MATRIX_EVAL="CC=clang-3.9 && CXX=clang++"
      before_install:
        - brew tap homebrew/nginx
        - brew update
        - brew install tclap
        - brew install ice
        - brew install -v ffmpeg --with-x265
        - brew install nginx-full --with-rtmp-module
        - brew cask uninstall oclint && brew install llvm@3.9 && brew link --overwrite llvm@3.9 --force
        - cp ~/build/Zialus/SD-Video-Streaming/extras/nginx.conf /usr/local/etc/nginx/
        - nginx
    - os: osx
      osx_image: xcode9
      env:
        - MATRIX_EVAL="CC=clang-4.0 && CXX=clang++"
      before_install:
        - brew tap homebrew/nginx
        - brew update
        - brew install tclap
        - brew install ice
        - brew install -v ffmpeg --with-x265
        - brew install nginx-full --with-rtmp-module
        - brew cask uninstall oclint && brew install llvm && brew link --overwrite llvm --force
        - cp ~/build/Zialus/SD-Video-Streaming/extras/nginx.conf /usr/local/etc/nginx/
        - nginx
